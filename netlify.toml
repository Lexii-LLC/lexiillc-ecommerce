[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Scheduled function for Clover sync (runs daily at 3 AM UTC)
[functions."app/api/cron/sync"]
  schedule = "0 3 * * *"
